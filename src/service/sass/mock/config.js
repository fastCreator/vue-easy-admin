const path = require('path')
const cwd = process.cwd()
const resolve = path.resolve

module.exports = {
  // configureWebpack: {
  //   devServer: {
  //     before: require('./mock-server')
  //   },
  // }
}